
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ЗаписатьКоординаты();
	
КонецПроцедуры

&НаСервере
Процедура ЗаписатьКоординаты()
	
	Дата = НачалоГода(ТекущаяДата());
	Пользователь = ПараметрыСеанса.ТекущийПользователь;
	
	Для й = 1 по 10000 Цикл
		
		МЗ = РегистрыСведений.ГеоКоординаты.СоздатьМенеджерЗаписи();
		МЗ.Период = Дата + й;
		МЗ.Сотрудник = Пользователь;
		Мз.Записать();
		
	КонецЦикла;
	
КонецПроцедуры