
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Элементы.Декорация1.Заголовок = ТипЗнч(Запись.Объект);
	СсылкаНаОбъект = ПолучитьНавигационнуюСсылку(Запись.Объект);
КонецПроцедуры
