
Процедура ПередЗаписью(Отказ)
	
	Если ЗначениеЗаполнено(Идентификатор) Тогда
		Запрос = Новый Запрос;
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	ШаблонАнкетыДляТП.Ссылка
		|ИЗ
		|	Справочник.ШаблонАнкетыДляТП КАК ШаблонАнкетыДляТП
		|ГДЕ
		|	ШаблонАнкетыДляТП.Идентификатор = &Идентификатор";
		Запрос.УстановитьПараметр("Идентификатор", Идентификатор);
		
		РезультатЗапроса = Запрос.Выполнить();
		ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
		Отказ = ВыборкаДетальныеЗаписи.Следующий();
	КонецЕсли;
	
КонецПроцедуры
