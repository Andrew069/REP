
Процедура ПередЗаписью(Отказ)
	
	Если ЗначениеЗаполнено(Идентификатор) Тогда
		Запрос = Новый Запрос;
		Запрос.Текст = 
		"ВЫБРАТЬ
		|	ВидыЗаданийТорговымПредставителям.Ссылка
		|ИЗ
		|	Справочник.ВидыЗаданийТорговымПредставителям КАК ВидыЗаданийТорговымПредставителям
		|ГДЕ
		|	ВидыЗаданийТорговымПредставителям.Идентификатор = &Идентификатор";
		Запрос.УстановитьПараметр("Идентификатор", Идентификатор);
		
		РезультатЗапроса = Запрос.Выполнить();
		ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
		Отказ = ВыборкаДетальныеЗаписи.Следующий();
	КонецЕсли;
	
КонецПроцедуры
